#include "Chaos.h"

/*
void Chaos::evolve() {


	dxdt = alpha*x*(1 - y) - (beta*z);
	dydt = -(gamma*y*(1 - pow(x, 2)));
	dzdt = mu*x;

	//op is outmatrix(always 1dim)
	px += (dxdt*dt);
	py += (dydt*dt);
	pz += (dzdt*dt);

}

void Chaos::draw() {}
*/